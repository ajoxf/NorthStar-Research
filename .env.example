# ---------------------------------------------------------------------------
# NordStar Pro — environment variables
#
# Copy to `.env.local` for local development. In production these are set in
# Vercel → Project → Settings → Environment Variables. Never commit real values.
#
# Anything still reading REPLACE_ME in production is NOT configured. The Cregis
# client and the notification providers deliberately throw a loud, explicit error
# rather than silently no-op'ing when they see a placeholder — a placeholder must
# never be mistaken for a working integration.
# ---------------------------------------------------------------------------

# --- Application ----------------------------------------------------------
# Public base URL of the deployment. Used for magic links, redemption emails,
# report links sent over email/WhatsApp, and Cregis success/cancel/callback URLs.
APP_BASE_URL=http://localhost:3000

# Secret used to sign session cookies and short-lived report URLs.
# Generate with: openssl rand -base64 32
AUTH_SECRET=REPLACE_ME_AUTH_SECRET

# Shared secret Vercel Cron sends as `Authorization: Bearer <value>`.
CRON_SECRET=REPLACE_ME_CRON_SECRET

# --- Database (Postgres via the Neon integration in the Vercel Marketplace) ---
# Both names match what that integration injects into the Vercel project
# automatically, so nothing needs wiring by hand in production. Set them yourself
# only for local development, or if you provision Postgres somewhere else.
#
# Pooled connection, used by the app at runtime.
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
# Direct (non-pooled) connection, used by Prisma Migrate only.
DATABASE_URL_UNPOOLED=postgresql://user:password@host/db?sslmode=require

# --- Nexus RAMP sign-in bridge --------------------------------------------
# RAMP authenticates with Supabase and knows nothing about this site, so a trial or a
# redeemed code here grants an entitlement and nothing anybody can sign into. Set these
# and the portal creates the RAMP account at the moment it grants access, using the email
# and password the person chose here — one credential, both sites, RAMP unchanged.
#
# From the RAMP project in the Supabase dashboard: Settings -> API Keys.
#   URL = Project URL
#   KEY = a secret key (`sb_secret_...`) from the "Publishable and secret API keys" tab.
#         NOT the publishable/anon key, which is the one safe for browsers.
#
# Set RAMP_SUPABASE_SECRET_KEY for an `sb_secret_...` key. The legacy `service_role` JWT,
# on the "Legacy anon, service_role API keys" tab, also works and goes in
# RAMP_SUPABASE_SERVICE_ROLE_KEY instead — it is read as a fallback so an existing
# deployment keeps working, but it stops working at the end of 2026 and cannot be rotated
# without signing every RAMP user out. Prefer the secret key.
#
# Either key bypasses row-level security and can read every user's trade data. It is only
# ever used server-side, from src/lib/product-auth.ts, and must never reach the browser.
#
# Leave them unset and nothing breaks: entitlements are still granted and recorded, the
# bridge reports itself as not configured in Payments -> Settings, and the nightly job
# reconciles everything the first time they are set.
RAMP_SUPABASE_URL=
RAMP_SUPABASE_SECRET_KEY=
# Fallback, only if you are still on the legacy key.
RAMP_SUPABASE_SERVICE_ROLE_KEY=
# The item slug the bridge applies to. Only change this if the product is renamed.
RAMP_ITEM_SLUG=nexus-ramp

# --- File storage (Vercel Blob) -------------------------------------------
BLOB_READ_WRITE_TOKEN=REPLACE_ME_BLOB_TOKEN

# One-time secret enabling /admin/bootstrap, which creates the FIRST admin account
# from the browser. Unset = the page is inert. Delete this once your admin exists.
ADMIN_BOOTSTRAP_SECRET=

# --- Hero video (optional) ------------------------------------------------
# The landing hero renders a poster still until a video URL is supplied here, so
# leaving these unset is a finished-looking state, not a broken one.
# Use a compressed, muted, seamlessly-looping MP4 (H.264). Keep it under ~6 MB —
# it loads on every visit, and bandwidth is billed wherever you host it.
# NEXT_PUBLIC_ prefixed because the hero is a client component.
NEXT_PUBLIC_HERO_VIDEO_URL=
NEXT_PUBLIC_HERO_POSTER_URL=

# --- Google sign-in (optional) --------------------------------------------
# Google Cloud Console → APIs & Services → Credentials → OAuth client ID (Web).
# Authorised redirect URI must be exactly:
#   {APP_BASE_URL}/api/auth/google/callback
# Only the openid/email/profile scopes are used, so no Google app review is needed.
# Leave unset and the Google button simply does not appear on the sign-in page.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Stripe (card subscriptions, $199/month, auto-renewing) ----------------
# STRIPE_PRICE_ID is a RECURRING monthly price of $199 — create it under
# Stripe → Product catalogue. A one-off price will not subscribe anyone.
# STRIPE_WEBHOOK_SECRET comes from the endpoint you register at
#   {APP_BASE_URL}/api/webhooks/stripe
# subscribed to: checkout.session.completed, invoice.paid,
#                customer.subscription.updated, customer.subscription.deleted
STRIPE_SECRET_KEY=REPLACE_ME_STRIPE_SECRET_KEY
STRIPE_PRICE_ID=REPLACE_ME_STRIPE_PRICE_ID
STRIPE_WEBHOOK_SECRET=REPLACE_ME_STRIPE_WEBHOOK_SECRET

# --- Cregis (crypto checkout) ---------------------------------------------
# Field names match the Cregis Developer Center dashboard.
# The client will paste real values straight into Vercel; leave these as
# placeholders in the repo.
CREGIS_PROJECT_ID=REPLACE_ME_PROJECT_ID
CREGIS_API_KEY=REPLACE_ME_API_KEY
CREGIS_BASE_URL=REPLACE_ME_BASE_URL

# Outbound relay. Cregis allowlists the IP that calls their API and Vercel has
# no fixed one, so production sends that single call through a relay on
# fixed-IP hosting whose address is on the Cregis allowlist.
#
# Set both or neither. Unset, the call goes direct to CREGIS_BASE_URL, which is
# what local development and any allowlisted machine should do.
#
# When the relay IS set it owns the Cregis hostname, and CREGIS_BASE_URL stops
# affecting checkout — worth knowing before debugging a base URL that appears
# to do nothing.
CREGIS_RELAY_URL=
CREGIS_RELAY_SECRET=


# Where public sample-report enquiries are sent. Falls back to EMAIL_FROM.
# Preview every template that uses these at /admin/emails.
# This is an INTERNAL address — the enquirer is never emailed automatically.
SAMPLE_REPORT_REQUEST_TO=

# --- Email delivery (Resend by default; swappable — see src/lib/notifications) ---
EMAIL_PROVIDER=console
RESEND_API_KEY=REPLACE_ME_RESEND_API_KEY
# Must be on a domain verified with the provider (SPF + DKIM), or sends are rejected.
EMAIL_FROM="NordStar Pro <team@fincoursa.com>"

# Signing secret for the Resend delivery webhook, from Resend -> Webhooks.
# Point the endpoint at {APP_BASE_URL}/api/webhooks/resend and subscribe to
# email.delivered, email.opened, email.clicked, email.bounced, email.complained.
# Without it the endpoint refuses every event and the Opened column stays at zero.
RESEND_WEBHOOK_SECRET=

# --- WhatsApp delivery ------------------------------------------------------
# DESCOPED. Reports are delivered by email only. The provider plumbing is still in
# src/lib/notifications/ so the channel can be turned back on without a rewrite, but
# nothing calls it and no member is offered it. Leave these unset.
# WHATSAPP_PROVIDER=console
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_FROM=
# TWILIO_WHATSAPP_TEMPLATE_SID=
